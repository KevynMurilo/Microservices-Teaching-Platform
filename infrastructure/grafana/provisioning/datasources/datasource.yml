# Auto-provision Prometheus as a data source in Grafana.
# No manual setup needed — Grafana reads this on startup.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
